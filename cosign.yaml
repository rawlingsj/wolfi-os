package:
  name: cosign
  version: 1.13.1
  epoch: 1
  description: Container Signing
  target-architecture:
    - all
  copyright:
    - license: Apache-2.0
      paths:
        - "*"
  dependencies:
    runtime:
      - ca-certificates-bundle

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - runs: |
      CGO_ENABLED=0 go build \
        -trimpath -ldflags "-buildid= -X sigs.k8s.io/release-utils/version.gitVersion=${{package.version}}" \
        -o "${{targets.destdir}}/usr/bin/cosign" ./cmd/cosign

advisories:
  CVE753:
    - timestamp: 2023-03-10T11:35:49.784051532Z
      status: fixed
      fixed-version: 2.1.5cg31-r0

secfixes:
  2.1.5cg31-r0:
    - CVE753
