package:
  name: cosign
  version: 2.1.5cg19
  epoch: 0
  description: Container Signing
  target-architecture:
    - all
  copyright:
    - license: Apache-2.0
      paths:
        - "*"
  dependencies:
    runtime:
      - ca-certificates-bundle
environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go
pipeline:
  - runs: |
      CGO_ENABLED=0 go build \
        -trimpath -ldflags "-buildid= -X sigs.k8s.io/release-utils/version.gitVersion=${{package.version}}" \
        -o "${{targets.destdir}}/usr/bin/cosign" ./cmd/cosign
advisories:
  CVE123ABC:
    - timestamp: 2023-02-08T16:39:41.008057Z
      status: fixed
      fixed-version: 2.1.5cg19-r0
secfixes:
  2.1.5cg19-r0:
    - CVE123ABC
